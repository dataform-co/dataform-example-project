config {
  type: "table",
  schema: "sources_2",
  tags: ["staging", "daily"],
  description: "Cleaned version of the raw crm.customer table."
}

select
  id as customer_id,
  first_name,
  last_name,
  email,
  country
from
  ${ref("crm", "customer")}

  